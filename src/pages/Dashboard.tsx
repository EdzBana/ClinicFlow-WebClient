import MainTemplate from "@/components/MainTemplate";

const Dashboard = () => {
  return (
    <div>
      <MainTemplate initialPage="Dashboard"></MainTemplate>
    </div>
  );
};

export default Dashboard;
